cmake_minimum_required(VERSION 3.28)
project(main)

# Add subdirectories for each library and the main executable
add_subdirectory(app)
add_subdirectory(logic)
add_subdirectory(gamef)
add_subdirectory(gmath)
